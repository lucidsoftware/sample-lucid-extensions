import {SliderConfiguration} from '../../utils';

export const SpiralRadiusSliderConfiguration: SliderConfiguration = {
    min: 0,
    max: 500,
    step: 1,
}

export const SpiralSpacingSliderConfiguration: SliderConfiguration = {
    min: 0,
    max: 2,
    step: 0.01,
}
